---
description: 
---

Rebuild the hero section using React + Tailwind + Framer Motion (and React Three Fiber if 3D is requested).
Include: animated gradient background, staggered headline entrance, subtitle delayed fade, CTA hover glow/scale, optional floating 3D object or parallax image.
After building, screenshot and verify smoothness before reporting done.